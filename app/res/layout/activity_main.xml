package com.example.projetoweb;

import android.content.Intent;
import android.os.Bundle;
import android.widget.Button;
import android.widget.TextView;
import android.widget.Toast;

import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {

    private String usuarioLogado;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        // Receber email do usuário logado
        usuarioLogado = getIntent().getStringExtra("usuarioEmail");

        // Botão voltar
        TextView btnBack = findViewById(R.id.btnBack);
        if (btnBack != null) {
            btnBack.setOnClickListener(v -> {
                Intent intent = new Intent(MainActivity.this, FormeLogin.class);
                intent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TASK);
                startActivity(intent);
            });
        }

        // Botão Locais
        Button btnLocais = findViewById(R.id.btnLocais);
        if (btnLocais != null) {
            btnLocais.setOnClickListener(v ->
                    Toast.makeText(MainActivity.this,
                            "Locais próximos: Unidade A, Unidade B, Unidade C",
                            Toast.LENGTH_LONG).show()
            );
        }

        // Botão Agendar
        Button btnAgendar = findViewById(R.id.btnAgendar);
        if (btnAgendar != null) {
            btnAgendar.setOnClickListener(v -> {
                Intent intent = new Intent(MainActivity.this, AgendarActivity.class);
                intent.putExtra("usuarioEmail", usuarioLogado);
                startActivity(intent);
            });
        }

        // Botão Histórico
        Button btnHistorico = findViewById(R.id.btnHistorico);
        if (btnHistorico != null) {
            btnHistorico.setOnClickListener(v -> {
                Intent intent = new Intent(MainActivity.this, HistoricoActivity.class);
                intent.putExtra("usuarioEmail", usuarioLogado);
                startActivity(intent);
            });
        }

        // 🔹 Botão Perfil do Usuário
        Button btnPerfil = findViewById(R.id.btnPerfil);
        if (btnPerfil != null) {
            btnPerfil.setOnClickListener(v -> {
                Intent intent = new Intent(MainActivity.this, PerfilActivity.class);
                intent.putExtra("usuarioEmail", usuarioLogado);
                startActivity(intent);
            });
        }
    }
}
